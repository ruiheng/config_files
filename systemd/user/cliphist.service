[Unit]
Description=Cliphist clipboard store (Wayland)
ConditionEnvironment=XDG_SESSION_TYPE=wayland

[Service]
Type=simple
ExecStart=wl-paste --watch cliphist store
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
